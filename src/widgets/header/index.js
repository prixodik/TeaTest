/**
 * Header markup lives in index.html (progressive enhancement).
 * Keep init as a no-op hook for future enhancements.
 * @param {HTMLElement} [_root]
 */
export function init(_root) {
  // static HTML — nothing to mount
}
